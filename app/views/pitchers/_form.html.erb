<% if @pitcher.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@pitcher.errors.count, "error") %> prohibited this pitcher from being saved:</h2>

    <ul>
    <% @pitcher.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= simple_form_for @pitcher do |f| %>  
  <%= f.input :name %>
  <%= f.input :email %>
  <%= f.button :submit %>
<% end %>
