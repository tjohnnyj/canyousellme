<% if @pitch.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@pitch.errors.count, "error") %> prohibited this pitcher from being saved:</h2>

    <ul>
    <% @pitch.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
<%= simple_form_for [:pitcher,@pitch] do |f| %>  
  <%= f.input :pitch_video_url %>
  <%= f.button :submit %>    
<% end %>               

