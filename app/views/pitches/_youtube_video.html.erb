<br>           
<br>  
<%= size %>
<object width="<%= pitch.return_size(size)[0] %>" height="<%= pitch.return_size(size)[1] %>">
  <param name="movie"
         value="<%= pitch.embed_video %>"></param>
  <param name="allowScriptAccess" value="always"></param>
  <embed src="<%= pitch.embed_video %>"
         type="application/x-shockwave-flash"
         allowscriptaccess="always"
          width="<%= pitch.return_size(size)[0] %>" height="<%= pitch.return_size(size)[1] %>"></embed>
</object>  
<%= link_to "view pitch", pitcher_pitch_path(pitch.pitcher_id, pitch.id) %>